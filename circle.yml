dependencies:
    pre:
        # Install the latest stable PlatformIO
        - sudo pip install -U platformio

test:
    override:
        -  platformio ci --board=esp32dev --board=esp_wroom_02 src/SHT1xToM2X.cpp
