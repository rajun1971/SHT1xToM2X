dependencies:
    cache_directories:
        - "~/.platformio"
    pre:
        # Install the latest develop PlatformIO
        - sudo pip install -U https://github.com/platformio/platformio-core/archive/develop.zip

test:
    override:
        - platformio ci --project-conf platformio.ini --board=esp32dev --board=esp_wroom_02 src/SHT1xToM2X.cpp
